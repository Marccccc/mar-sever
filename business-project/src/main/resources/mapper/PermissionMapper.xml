<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mar.dao.PermissionDao">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.mar.entity.Permission">
		<id column="code" property="code" />
		<result column="name" property="name" />
		<result column="description" property="description" />
	</resultMap>
	
	<select id="listPermissionByUser" resultMap="BaseResultMap" >
      SELECT p.* FROM user_role ur LEFT JOIN role_permission rp ON ur.role_id=rp.role_id
        LEFT JOIN permission p ON rp.permission_code=p.code
        WHERE ur.user_id=#{userId}
	</select>

</mapper>
